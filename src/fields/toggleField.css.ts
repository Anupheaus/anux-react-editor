import { style, flex } from 'anux-react-styles';

export default {

    root: style(flex.full.wrap.align({ vertical: 'center' })),

    label: style(flex.full),

    span: style(flex.content),

};
